# How to use ?

### `cd` into project folder and run : 
``` bash
python -m venv .env
source .env/bin/activate
pip install maturin
```

### Then to build the library run: 
``` bash
maturin develop
```

### Run the python app: 
``` bash
python3 main.py
```